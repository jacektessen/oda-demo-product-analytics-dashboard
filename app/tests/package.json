{
  "name": "oda-api-tests",
  "version": "1.0.0",
  "description": "API tests for ODA stats service",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report",
    "wait-for-api": "node waitForApi.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.1",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.2"
  }
}